import { Button } from "@radix-ui/themes";

const DeleteIssueButton = ({ issueId }: { issueId: number }) => {
  return <Button color="red">Radera ärende</Button>;
};

export default DeleteIssueButton;
